
### Installation
1. Clone the repository: git clone [repository URL]
2. Navigate to the project directory:  cd [project name]
3. Install dependencies: npm install

### Running the App
Instructions on how to run the app on different platforms.

1. To run on a web browser: ionic serve
2. To run on android emulator (make sure enviorement is configured): ionic capacitor run android 
3. To build for iOS: ionic capacitor build ios
4. To build for Android: ionic capacitor build android
